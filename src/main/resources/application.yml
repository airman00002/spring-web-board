server:
  port: 8080

---
spring:
  profile:
    active: local-dev
    
---
spring:
  profile: local-dev
  datasource:
    url: jdbc:postgresql://postgresql:5432/Blogs
    username : postgres
    password :  password
  jpa:
    properties:
      hibernate:
       format_sql: true
    show-sql: true
    hibernate.ddl-auto : update
      
